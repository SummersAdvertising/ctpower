= FEATURE new =
<%= simple_form_for(@feature, :url => admin_category_vehicle_features_path(params[:category_id] , params[:vehicle_id], @feature), html: { id: "postthisfeature" } ) do |f| %>

title：<%= f.input_field :title %><br>
context：<%= f.input_field :context %><br>

  <%= f.submit 'Save' %><br>
<% end %>

<%= link_to 'return' ,admin_category_vehicle_features_path(params[:category_id], params[:vehicle_id]) %>