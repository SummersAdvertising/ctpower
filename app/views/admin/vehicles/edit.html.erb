
<header>
  <h2 class="left">編輯產品</h2>
  <nav class="left">
    <%= link_to "Basic", edit_admin_category_vehicle_path(@vehicle.category, @vehicle)%>
    <%= link_to "Specs", admin_category_vehicle_specs_path(@vehicle.category, @vehicle)%>
    <%= link_to "Features", admin_category_vehicle_features_path(@vehicle.category, @vehicle)%>
    <%= link_to "Colors", admin_category_vehicle_colors_path(@vehicle.category, @vehicle)%>
    <%= link_to "Accessories", admin_category_vehicle_accessories_path(@vehicle.category, @vehicle)%>
    <%= link_to "VehicleSlider", admin_category_vehicle_slider_path(@vehicle.category, @vehicle)%>
    <%= link_to "VehicleDMFile", admin_category_vehicle_dm_path(@vehicle.category, @vehicle)%>
    <%= link_to "VehiclePhotoGallery", admin_category_vehicle_photo_path(@vehicle.category, @vehicle)%>
  </nav>
</header>

<article>
  <div class="hgroup">
      <h3>
      </h3>
  </div>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
  <div class="form">
  
    <%= simple_form_for(@vehicle, :url => admin_category_vehicle_path(params[:category_id] , @vehicle), html: { id: "patchthisvehicle", :multipart => true } ) do |f| %>
    名稱：<%= f.input_field :name %><br>
    標題顏色：<%= f.input_field :title_color %><br>
    價格：<%= f.input_field :price %><br>
    補助價：<%= f.input_field :price_for_sale %><br>
    CF：<%= f.input_field :cf %><br>
    簡介：<%= f.text_area :description %><br>

    <% if @gallery_count > 0 %>
      <%= render 'admin/common/show_by_type', target: @vehicle, target_title: '圖', type: 'VehicleCover' %>
    <% else %>
      <%= render 'admin/common/upload_by_type', target_title: '圖' %>
    <% end %>
    
    <%= f.submit 'Save' %><br>

  <% end %>
  
  </div>

  
  <!-- <div class="button"> 
    <a href="javascript: document.getElementById('patchthisvehicle').submit();" class="right highlight" data-no-turbolink = true >儲存<img src="<%= image_path("save.png") %>"></a>
    
   </div> -->

</article>

<%= link_to "返回", admin_category_path(@vehicle.category) %>

<script type="text/javascript">
$(document).ready(function(){

    
});
</script>