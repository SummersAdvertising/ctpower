<% @q = Faq.order(:box_id).search() %>
<%= search_form_for @q, url: search_faqs_path, class: "form-inline quick-search-form" , method: :post  do |f| %>
    <div class='form-group'>
      <%= f.search_field :question_cont, class: "form-control", placeholder: "Search here" %>
    </div>
    <%= f.submit('搜尋' , id: "quick-search", class: "btn btn-custom") %>
 <% end %>